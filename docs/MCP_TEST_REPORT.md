# MCP test report

Service-layer tests cover seeded decks, CRUD, reviews, quiz generation, CSV export, JSON import, and persistence. Run `scripts/test.ps1` after installation for the local verification suite.
